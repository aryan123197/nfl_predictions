# --- Phase 1: ingestion ---
pandas>=2.2,<3.0
requests>=2.31
sqlalchemy>=2.0
psycopg2-binary>=2.9   # Postgres driver (not needed for local sqlite dev)
python-dotenv>=1.0

# --- Phase 3: features (injury impact config) ---
pyyaml>=6.0

# --- Phase 4: ML training ---
xgboost>=2.0,<3.0
scikit-learn>=1.4
joblib>=1.3

# --- Phase 5+: not required yet, listed here so the file matches the
#     full roadmap; install later ---
# lightgbm>=4.3
# mlflow>=2.11        # Phase 8 (MLOps) -- Phase 4 versions models with a plain metadata.json instead
# shap>=0.45
# fastapi>=0.110
# uvicorn>=0.29

# --- testing ---
pytest>=8.0
